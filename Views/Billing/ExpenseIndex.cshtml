@model IEnumerable<ExpenseBill>
@{ ViewData["Title"] = "Expense Bills"; }

<div class="page-toolbar">
    <div></div>
    <a asp-action="CreateExpense" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Create Expense Bill</a>
</div>

<div class="table-responsive shadow-sm">
    <table class="table table-striped">
        <thead>
        <tr><th>Date</th><th>Description</th><th>Total (MMK)</th><th class="text-end">Actions</th></tr>
        </thead>
        <tbody>
        @foreach (var b in Model)
        {
            <tr>
                <td>@b.BillDate.ToShortDateString()</td>
                <td>@b.Description</td>
                <td>@b.TotalAmount.ToString("N0")</td>
                <td>
                    <div class="action-buttons">
                        <a asp-action="ExpenseDetails" asp-route-id="@b.ExpenseBillId" class="btn btn-sm btn-info">Details</a>
                    </div>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
